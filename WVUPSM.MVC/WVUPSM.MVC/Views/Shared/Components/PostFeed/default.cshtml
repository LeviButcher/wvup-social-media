@model List<UserPost>

<section class="post-container">
    @if (Model != null)
    {
        @foreach (UserPost post in Model)
        {
            @await Html.PartialAsync("_UserPost", post)
        }
    }
    else
    {
        <h4>No posts</h4>
    }
</section>